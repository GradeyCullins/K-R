#include <stdio.h>

int main() {

	/* doesn't compile - ex. 2-2 */
	int i = 0;
	for (; i < lim - 1; ++i) {
		c = getchar();
		if (c == '\n')
			break;
		if (c == EOF)
			break;
		s[i] = c;
	}

	
	return 0;
}
